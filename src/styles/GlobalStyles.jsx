import React from 'react';
import './global.css';

// Simple wrapper component so App can import <GlobalStyles />.
// Keeps CSS import colocated; returns null because styles are global.
export default function GlobalStyles() {
  return null;
}
